{% trans_default_domain 'Front' %}
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
{{ knp_menu_render('main', {template: 'sitemap_menu.xml.twig'}) }}
{% for url in urls %}
        <url>
            <loc>{{ absolute_url(url.loc) }}</loc>
        </url>
    {% endfor %}
</urlset>